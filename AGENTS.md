# AGENTS.md - 测试用例生成与 IMA 知识库联动行为规范

本文件定义 Agent 在处理测试用例生成（`create-testcases`）任务时的行为准则与自动联动规范。

## 核心行为准则：IMA 知识库优先联动

当用户要求生成测试用例（或基于某个业务功能、模块、改动设计用例）时：

1. **自动知识库检索**：
   - 只要用户提供了业务模块、功能名称，或者未提供完全自包含的需求文档文件，**Agent 必须自动优先调用 `ima-skill` 搜索检索 IMA 知识库和笔记**中的已有文档。
   - 检索顺序：
     1. 调用知识库检索（`search_knowledge_base` / `search_knowledge`）查找相关需求文档、PRD、接口说明或设计方案。
     2. 调用笔记检索（`search_note`）查找用户是否有相关的个人笔记、待办备忘或架构讨论记录。
2. **提取已有文档内容**：
   - 对于知识库中检索到的相关条目，调用 `get_media_info` 获取原文直链并提取内容；若条目为笔记类型（`media_type=11`），调用 `get_doc_content` 读取正文。
   - 对于个人笔记，调用 `get_doc_content` 读取正文纯文本。
3. **在已有文档基础上生成用例**：
   - 将知识库中检索到的已有功能逻辑、字段规则、历史用例作为基线；
   - 结合用户当前提出的新增诉求、变更点或测试重点，进行增量与回归用例设计；
   - 确保用例不仅覆盖新改动，还覆盖与已有文档中既有逻辑的联动与边界校验。
4. **透明呈现与溯源**：
   - 在“需求分析摘要”和导出的用例文件中，清晰标注所参考的 IMA 知识库条目名称及版本。
   - 若在知识库中未检索到相关已有文档，明确向用户说明并基于当前提供的信息继续生成。
5. **增量差异（Diff）分析与一体化呈现**：
   - 提取出知识库历史基线后，必须通过 `scripts/ima-bridge.mjs diff` 对当前新需求进行结构化差异比对。
   - 在向用户汇报《需求分析》时，**必须将【宏观需求分析摘要】与【微观增量差异矩阵（Diff）】合体呈现**，明确区分 🟢 新增对象、🟡 变更逻辑与 🔵 需重点回归的存量范围，待用户一次性确认后再生成测试用例。
6. **用例生成后主动发起知识库双向归档确认**：
   - 测试用例导出完成后，Agent **必须在最终回复中主动向用户询问是否归档至 IMA 知识库**，提供三种选项（选项 A：独立新版本文件 `--mode new-file`；选项 B：合并演进原脑图 `--mode merge`；选项 C：暂不归档）。
   - **推荐策略决策**：常规单一功能小迭代推荐选项 A；**如果一次迭代改动跨度较大、跨越多个业务子系统，需长期维护单模块的活文档测试基线，推荐采用方案 2（按模块拆分定向合并）**，引导用户分别定向合并融入各子系统的已有基线脑图中。
   - **文件归档命名规范**：入库文件名必须严格遵循**原文件名或带有版本号**（例如：`{原脑图名}.xmind` 或 `{原脑图名}_{版本号}.xmind`、`{版本号}{用例名}.xmind`），严禁使用 `_merged` 等内部临时命名。
   - 用户确认后，调用 `scripts/ima-bridge.mjs push` 自动执行归档，形成测试资产沉淀闭环。

## 严格临时文件与中间产物清理规范（强制执行）

1. **产物唯一性**：除最终导出的三类文件（`.md`、`.xlsx`、`.xmind`）存放在 `exports/<业务范围名称>/` 外，严禁在 `exports/` 或工作区任何位置残留任何中间提取文件、下载副本或抓取缓存。
2. **立即清理**：在解析在线原型（如 Mockplus 抓取产生的 `exports/mockplus`）或下载临时文件时，一旦分析或用例导出完成，**必须在当前轮次结束前立即自动彻底删除**，绝不得遗留。
